# 更新日志

## v1.2.0 (未发布)

### 新增
- 程序重命名：PopNotification → BubbleMind
- 可执行启动器 `BubbleMind.exe`（MinGW 编译，嵌入 ICON 图标）
- `[linkmap]` 自动链接区域标记：指定源词与目标关键词映射，区域内自动生成链接
- 知识内容编辑区域回车切换编辑模式快捷键
- 后退导航功能（◀ 按钮 / Alt+←），支持多步回退
- 按钮快捷键标识（保存 Ctrl+S、编辑 ↵）

### 优化
- 导入 `expand_linkmap` 采用延迟加载避免循环导入
- 任务栏图标改用 PIL 加载 PNG，确保兼容性
- 启动脚本删除 `启动.bat` / `启动.vbs`，统一为 `BubbleMind.exe`

### 修复
- 编辑器标题未随重命名更新
- 编辑器窗口图标未正确设置
- `[linkmap]`、`editor.save_desc`、`markup.code`、`markup.item` 翻译键缺失
- 按钮显示原始 key 字符串 (`editor.save_desc`)

### 新增
- 学科/章节目录页：点击树节点时展示该范围下的关键词列表与 Pin 词索引
  - 学科页面：按章节分组展示全部关键词，支持点击跳转
  - 章节页面：展示该章节下所有关键词和 Pin 词链接
- `[[pin:词]]` Pin 词标记：在知识内容中标记重点词汇，自动收集到目录页
- 多语言支持（中文/English/日本語/한국어），编辑器底部栏语言切换器
  - 编辑器和弹窗中渲染为紫色 📌 样式
  - 目录页中以可点击链接展示，点击跳转到包含该 Pin 的关键词
- 知识内容中的 `[[keyword]]` 链接自动添加为相关话题
  - 保存关键词时自动扫描内容中的链接并补全相关话题列表
  - 加载已有内容时同样处理，兼容旧数据
- 到期卡片复习提醒弹窗：不再直接弹出卡片，改为提示当前待复习数量
  - 点击「开始复习」后依次弹出卡片
  - 可选 5/15/30 分钟及 1 小时后提醒
  - 自定义分钟数输入
  - 「今天不提醒」一键跳过

### 优化
- 编辑器浏览模式直接显示图片（基于 Pillow 渲染），不再显示文本标记
- 插入图片时编辑器仅显示文件名，路径自动解析
- 图片插入增加实时状态反馈（选择中 → 复制中 → 完成）
- 添加学科按钮始终显示在左侧按钮栏
- `+ 章节` / `+ 关键词` 按钮按父子层级顺序排列
- 添加新学科后知识树不再自动全部展开

### 修复
- 知识卡片弹窗中图片无法显示的 bug（popup 正则条件笔误）
- 编辑器浏览模式下切换到编辑模式插入图片失败的问题
- 编辑器中 `[[keyword|display]]` 链接语法支持（显示自定义文字）

### 其他
- 启动时自动清理未被引用的孤儿图片
- 自动更新检测机制（对比 GitHub Releases 版本号）
- 更新后首次打开展示更新说明弹窗
